# 2026-09-22 原型编辑器开发日志

本页记录当天 GrapesJS 与原型编辑器相关的实际进度。选型分析见[原型编辑器选型](../architecture/prototype-editor-selection.md)；需求范围见[需求日志中的 REQ-010](requirements-log.md)。状态以提交与当前工作树分别描述，未把工作中的代码视为交付。

## 已提交及验证

| 提交 | 实际变更 | 验证与边界 |
| --- | --- | --- |
| `13938e9` | 建立独立 GrapesJS 原型试验、原始工程保存、截图确认与 PRD 快照交接。 | 单页试验；不等于官网完整编辑器。具体测试记录见[实施说明](agent-plugin-workbench-implementation.md)。 |
| `326288e` | 在常用 `/prd-studio` 插件面板开放入口，并给 GrapesJS 与 Puck 增加清空画布操作。 | 已完成当批浏览器回归；两个宿主的资源与草稿上下文仍未统一。 |
| `c00340d` | 扩充 GrapesJS 内核编辑模块和安全组件，支持最多 4 页的页面管理、逐页截图与版本化 PRD 交接；增加设备预览、素材、历史与导出。 | GrapesJS 专项 Python 10 项、前端扩展 35 项、类型检查、生产构建、严格文档检查及两个 Chromium 脚本通过。4 页、截图及工程大小仍受现有 NexusOS 资源契约限制。 |
| `7d595f1` | 调整两个原型编辑器的控件与界面，增加相关依赖和回归脚本改动。 | 本日志未对该提交重新运行验证；不能由提交存在推断当前未提交改动已通过测试。 |

## 选型方向与进行中的改动

用户选择自托管 **GrapesJS 官网开源 Webpage Demo 路线**：以开源 GrapesJS、Webpage Preset 与按需选择的社区插件组合，目标是让插件内的组件和编辑体验更接近官网演示。这与有独立许可证和免费档额度的 Grapes Studio SDK 不同。[官网演示](https://grapesjs.com/demo)、[演示源码](https://github.com/GrapesJS/grapesjs/blob/gh-pages/demo.html)、[Webpage Preset](https://github.com/GrapesJS/preset-webpage)、[SDK 价格](https://grapesjs.com/sdk/pricing)。

当前**未提交**的工作树中，已加入 Webpage Preset 及基础块、表单、Flexbox、背景样式、导出等开源插件依赖，并在 GrapesJS 插件自身扩充经典组件；同时存在 Puck 原型编辑器的组件与界面调整。这些是进行中的实现，**尚未完成整体验证与提交**。针对新增表单／提示组件的浏览器拖放回归尚未通过，不能宣称官网 Demo 功能已经完整嵌入，也不能把此前 `c00340d` 的通过结果套用到当前工作树。

按用户本次指示，后续仅做文档：保留上述代码现场，不继续修改、回退或提交代码。

## 剩余缺口

- 复核官网 Demo 各插件与当前 GrapesJS 版本的兼容性，完成新增组件的真实拖放、保存重开、截图及 PRD 交接回归；逐项记录启用和排除的功能。Webpage Preset 本身并不自动打包全部演示插件。[Preset 说明](https://github.com/GrapesJS/preset-webpage)。
- 目前脚本、外部媒体与任意 HTML 导入受 NexusOS 同源编辑安全边界限制；若要开放此类能力，需要先设计独立隔离和资源协议，不能直接等同于官网公开演示。
- GrapesJS 仍是手工原型插件，尚无 Agent 组件级生成／修改。旧 `/prd-studio` 与 `/studio` 的草稿和交接上下文还未统一；Puck 与 GrapesJS 的同任务编辑效率、性能和真实模型交接尚无并排验收。
- Penpot 尚未集成；是否投入独立部署和设计文件适配，按[选型文档](../architecture/prototype-editor-selection.md)中的触发条件决定。
