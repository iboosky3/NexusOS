# 问题与解决记录

本文持续记录 NexusOS 在**开发、集成、部署和实际使用**过程中遇到的工程问题，以及对应的定位过程、解决方案、验证结果与遗留风险。它不是理想化的功能清单，也不把尚未发生的问题写成“线上事故”。

问题来源分为四类：

- `开发测试`：由单元测试、集成测试或代码审查实际发现；
- `场景验证`：在 Nexus PRD 等参考工作流中实际复现；
- `基准测试`：在可重复数据集或规模压测中观察到；
- `部署运行`：在本地 VM、容器、预发布或生产运行中观察到。

每条记录都必须保留首次发现时间。后续方案变化直接追加处理记录，不覆盖原始判断；如果最终证明根因不同，应明确更正。

## 状态说明

| 状态 | 含义 |
| --- | --- |
| 已解决 | 修复已合入，并由自动化测试或可重复步骤验证 |
| 已缓解 | 主要影响已被控制，但长期方案尚未完成 |
| 观察中 | 已有修复，需要继续观察真实负载或更多样本 |
| 待解决 | 已确认问题，尚无可接受的修复 |

## 问题索引

| 编号 | 首次发现 | 来源 | 问题 | 状态 |
| --- | --- | --- | --- | --- |
| NX-001 | 2026-05-24 | 开发测试 | Skill 清单缺省字段触发类型校验失败 | 已解决 |
| NX-002 | 2026-06-12 | 开发测试 | 测试发现命令成功退出但实际执行 0 项 | 已解决 |
| NX-003 | 2026-06-12 | 场景验证 | 一次性加载全部 Skill 导致上下文被指令淹没 | 已解决 |
| NX-004 | 2026-07-18 | 开发环境 | 本机缺少 Go、Rust、Node.js 与 Docker 工具链 | 已缓解 |
| NX-005 | 2026-07-18 | 集成测试 | 远程任务重试可能重复执行有副作用的工作 | 已解决 |
| NX-006 | 2026-07-26 | 场景验证 | 固定 PRD 分数无法反映真实产物质量 | 已解决 |
| NX-007 | 2026-08-01 | 部署验证 | 可选 HTTP 与容器依赖在当前开发机不可用 | 已缓解 |
| NX-008 | 2026-08-15 | 场景验证 | 无界 Reflection 可能反复重写并消耗预算 | 已解决 |
| NX-009 | 2026-08-16 | 安全审查 | 连接池复用可能造成数据库租户上下文串用 | 观察中 |
| NX-010 | 2026-08-22 | 场景验证 | Studio 无法从内部运行状态稳定构造可解释视图 | 观察中 |
| NX-011 | 2026-08-23 | 集成测试 | 外部 Agent 的重复或乱序事件会让任务状态回退 | 已解决 |
| NX-012 | 2026-08-23 | 开发测试 | 首次统一 Lint 暴露跨模块风格漂移 | 已解决 |
| NX-013 | 2026-08-29 | 场景验证 | Studio 静默使用样例时容易被误认为真实监控 | 已解决 |
| NX-014 | 2026-08-29 | 场景验证 | 长任务在全局上下文充足时仍被分区硬上限拒绝 | 已解决 |
| NX-015 | 2026-08-30 | 故障推演 | 模型供应商瞬时故障直接终止整条 Agent 任务 | 观察中 |
| NX-016 | 2026-08-30 | 安全测试 | Memory 全局召回可能混入其他租户或过期内容 | 已解决 |
| NX-017 | 2026-09-05 | 部署验证 | 单一健康端点无法区分进程存活与接流准备 | 已解决 |
| NX-018 | 2026-09-05 | 开发测试 | API 已安装但测试客户端缺少传输依赖 | 已解决 |
| NX-019 | 2026-09-05 | 实际使用 | CLI 从仓库外启动时无法定位 Agent 与 Skill 清单 | 已解决 |
| NX-020 | 2026-09-05 | 实际使用 | Windows 终端中的中文诊断结果显示乱码 | 已解决 |
| NX-021 | 2026-09-06 | 开发测试 | Mypy 虽已列为依赖但从未进入质量门 | 已解决 |
| NX-022 | 2026-09-09 | 发布封版审计 | 许可证正文因人工拷贝出现缺词和重复词 | 已解决 |
| NX-023 | 2026-09-09 | 发布封版审计 | Windows 工作区中新写入文件触发 Ruff 读取异常 | 已缓解 |
| NX-024 | 2026-09-09 | 发布封版审计 | 无隔离 Wheel 构建因本机缺少构建后端而失败 | 已解决 |

## NX-001 · Skill 清单缺省字段触发类型校验失败

- **首次发现**：2026-05-24
- **来源**：开发测试
- **现象**：加载未声明 `examples` 的合法 Skill 时，清单解析失败；显式写入空列表则可以通过。
- **影响**：最小 Skill 包无法按约定加载，作者被迫填写没有业务意义的占位字段。
- **定位过程**：使用最小清单夹具复现，确认 YAML 内容本身合法；继续检查发现模型默认值是元组，而字段校验器只接受列表。
- **根因**：内部不可变默认值与外部 YAML 数组契约不一致，默认值也经过了同一类型校验。
- **解决方案**：把外部集合字段默认值统一为空列表，在模型构造后再转换为不可变表示；增加“省略可选集合字段”的回归用例。
- **验证方式**：最小清单、完整清单、错误类型三组测试均通过，版本化仓库可以同时加载多个 Skill 包。
- **预防措施**：清单模型的默认值必须使用与序列化格式一致的类型；新增字段同时提供最小契约测试。
- **遗留风险**：未来接入 Pydantic 或其他 Schema 框架时，默认值校验策略可能变化，需要复用同一夹具重新验证。

## NX-002 · 测试发现命令成功退出但实际执行 0 项

- **首次发现**：2026-06-12
- **来源**：开发测试
- **现象**：`unittest discover` 返回退出码 0，但输出为 `Ran 0 tests`。
- **影响**：CI 或开发者可能把“没有收集到测试”误判为测试通过，让回归问题进入后续分支。
- **定位过程**：指定单个测试文件可以执行，目录发现模式无法递归；检查包结构后发现测试目录缺少包标记。
- **根因**：当前 Python 发现规则要求递归目录可导入，而测试目录没有 `__init__.py`；同时校验流程只检查退出码，没有检查收集数量。
- **解决方案**：为测试目录补充包标记，固定仓库级测试命令，并增加架构测试保证关键测试模块可发现。
- **验证方式**：同一发现命令由 0 项恢复为 13 项且全部通过；后续新增套件继续使用同一入口。
- **预防措施**：CI 日志必须显示测试数量；不得用单纯的退出码 0 作为“测试已覆盖”的证据。
- **遗留风险**：如果未来迁移 pytest，需要重新设置最少收集数或等价保护。

## NX-003 · 一次性加载全部 Skill 导致上下文被指令淹没

- **首次发现**：2026-06-12
- **来源**：场景验证、基准测试
- **现象**：在 PRD 工作流中把所有 Skill 正文拼入提示词后，真正的用户需求与中间产物占比明显下降；目录扩大时 Token 线性增长。
- **影响**：模型成本和延迟上升，注意力被无关规则分散，长目录最终会超过上下文窗口。
- **定位过程**：分别运行 All Skills、关键词选择和混合路由，记录选中 Skill 数、指令 Token、Recall@K 与排序解释。
- **根因**：Skill 发现和 Skill 正文加载没有分层；系统把“可能有用的能力”误当成“本次必须注入的指令”。
- **解决方案**：采用三级渐进加载：启动阶段只读元数据，路由后读取 `SKILL.md`，只有执行需要时才加载参考资料；增加 Token 预算器和确定性裁剪。
- **验证方式**：版本化路由数据集与 100～10,000 Skill 合成规模测试可以重复比较质量和 Token；当前小样本显示相关 Skill 可被保留，无关正文不再进入上下文。
- **预防措施**：路由变更同时报告质量和 Token，禁止只用 Token 降幅证明方案有效。
- **遗留风险**：合成负样本不等同真实困难样本；进入生产前仍需用匿名化真实查询做离线回放。

## NX-004 · 本机缺少多语言与容器工具链

- **首次发现**：2026-07-18
- **来源**：开发环境
- **现象**：当前 Windows 开发机可以运行 Python，但找不到 Go、Cargo/Rust、Node.js 和 Docker 命令。
- **影响**：Go Runtime、Rust Router、TypeScript Studio 和 Compose 无法在本机完成编译或启动验证。
- **定位过程**：检查命令路径与版本，区分“代码未通过”与“验证工具不存在”，避免把静态检查写成编译成功。
- **根因**：开发机只配置了 Python/Git 基础环境，多语言实现先于统一构建环境落地。
- **解决方案**：保留可运行的 Python 规范实现；本机先执行契约、源码结构和 YAML/JSON 校验；后续由固定版本的 Linux CI 承担 Go、Rust、Node.js 和容器构建。
- **处理记录（2026-08-23）**：已增加固定版本的跨语言 CI 矩阵和仓库级验证器，覆盖 Go Race Detector、Rust Clippy、Studio 类型/生产构建、MkDocs 严格构建与 Compose 展开。
- **验证方式**：Python 回归和仓库级契约检查可在本机执行；缺少工具链的组件仍明确标记为“待首次远程 CI 验证”，不因工作流文件存在就宣称已经编译。
- **预防措施**：提交新语言组件时必须同步增加 CI Job，并固定编译器版本。
- **遗留风险**：CI 尚未建立前仍存在平台编译差异，本问题维持“已缓解”而不是“已解决”。

## NX-005 · 远程任务重试可能重复执行有副作用的工作

- **首次发现**：2026-07-18
- **来源**：集成测试、故障推演
- **现象**：客户端超时后重发任务，首次请求可能已经执行成功；第二次请求会再次调用写数据库、发消息或外部工具。
- **影响**：产生重复工单、重复通知、重复计费或不一致的运行状态。
- **定位过程**：用并发请求和相同任务输入复现，发现仅依赖 `task_id` 无法区分合法的新尝试与网络层重复提交。
- **根因**：远程 Runtime 定义了重试次数，却没有把幂等性纳入传输契约和结果缓存。
- **解决方案**：任务契约强制携带 `run_id`、`task_id` 和 `idempotency_key`；Go Runtime 合并进行中的相同请求并复用已完成结果；MCP Gateway 在租户范围内重复检查工具调用键。
- **验证方式**：并发重复请求测试只调用一次实际 Handler，所有调用者得到同一结果；数据库对工具幂等键增加唯一约束。
- **预防措施**：有副作用的 Tool 不允许依赖自动生成且不可追踪的键；重放操作必须显式选择复用或创建新键。
- **遗留风险**：单进程内存缓存不能覆盖实例重启和多副本，生产部署需要 Redis 或 PostgreSQL 持久化幂等记录。

## NX-006 · 固定 PRD 分数无法反映真实产物质量

- **首次发现**：2026-07-26
- **来源**：场景验证
- **现象**：演示流程无论输入和生成内容如何都给出固定质量分，缺少核心章节的 PRD 也可能显示“通过”。
- **影响**：质量门失去可信度，Replanner 没有具体的修订目标，用户无法解释分数来源。
- **定位过程**：构造缺少目标、用户流程和验收标准的产物，比较固定评分与人工检查结果。
- **根因**：早期端到端样例用常量代替了评估端口，评审结果没有结构化维度和阻断规则。
- **解决方案**：引入版本化 Rubric，分别计算清晰度、完整性、可行性、一致性和证据覆盖；核心章节缺失优先阻断，并输出具体失败维度。
- **验证方式**：完整夹具达到阈值，删除任一核心章节后稳定失败；同一产物重复评估结果一致。
- **预防措施**：演示桩不得进入正式质量字段；未来模型评审只能补充判断，不能覆盖确定性阻断项。
- **遗留风险**：确定性规则可能鼓励格式满足而不是真实业务价值，需要通过人工盲评校准权重。

## NX-007 · 可选 HTTP 与容器依赖在当前开发机不可用

- **首次发现**：2026-08-01
- **来源**：部署验证
- **现象**：FastAPI 未安装，Docker 命令不可用，无法在当前开发机直接启动 HTTP API 和 Compose 栈。
- **影响**：核心工作流可以进程内运行，但网络边界、镜像健康检查和基础设施联调不能在本机形成完整证据。
- **定位过程**：分别检查 Python 可选依赖导入和 Docker 客户端；API 序列化测试可运行，服务启动失败是环境缺失而非业务异常。
- **根因**：核心包刻意保持轻依赖，而本机没有安装 `api` 可选依赖与容器运行时。
- **解决方案**：把 FastAPI 保持为可选 extra，核心测试不依赖 Web 框架；使用 PyYAML 和契约测试验证 Compose 配置；把真正的启动探针交给后续 CI 集成环境。
- **处理记录（2026-08-23）**：CI 增加 `api` 可选依赖安装、MkDocs 严格构建、Studio 生产构建和 `docker compose config`，先覆盖构建与配置层证据。
- **处理记录（2026-09-05）**：当前开发机已安装 `api` 可选依赖，FastAPI TestClient 实际验证 Liveness、Readiness、创建、列表和详情接口；容器运行仍待 Docker 环境。
- **验证方式**：核心 Orchestrator 和 API 响应映射测试通过，Compose YAML 可解析；首次远程 CI 与真正的容器健康检查仍待外部环境执行。
- **预防措施**：文档把“源文件校验”“构建通过”“服务可启动”作为三种不同证据，不混用结论。
- **遗留风险**：依赖版本冲突和容器启动顺序尚未被真实运行覆盖，本问题保持“已缓解”。

## NX-008 · 无界 Reflection 可能反复重写并消耗预算

- **首次发现**：2026-08-15
- **来源**：场景验证、故障推演
- **现象**：评审失败时若只返回“继续优化”，写作 Agent 会重复生成整份 PRD；质量没有提升时流程没有稳定终点。
- **影响**：Token 和时间不可控，已正确的章节被反复修改，Run 可能长期占用 Worker。
- **定位过程**：让评审持续返回相同失败维度，观察无上限重试策略会重复构造相同任务图。
- **根因**：Reflection 被实现为自由文本循环，没有修订范围、轮次预算和最终状态。
- **解决方案**：把失败维度映射成 Research、Requirements 或 Technical 修订任务，只重走受影响路径；默认最多两轮，仍不通过则把 Run 标记为 `blocked`。
- **验证方式**：测试覆盖一次修复后通过和持续失败达到上限两条路径；每轮任务 ID、评审分与事件均可追踪。
- **预防措施**：任何循环型 Agent 能力必须同时声明最大轮次、最大 Token、退出状态和人工接管点。
- **遗留风险**：当前依赖维度级映射，尚不能精确判断段落级证据依赖，可能执行多余修订。

## NX-009 · 连接池复用可能造成数据库租户上下文串用

- **首次发现**：2026-08-16
- **来源**：安全审查
- **现象**：如果使用会话级变量保存租户 ID，连接归还池后可能被另一租户复用；遗漏 `WHERE tenant_id` 的查询也可能读取跨租户数据。
- **影响**：造成高严重度的数据越权和审计污染。
- **定位过程**：审查所有带 `tenant_id` 的表和连接生命周期，发现应用过滤与会话级设置都依赖每个调用点正确清理。
- **根因**：租户隔离只停留在应用约定，没有数据库级默认拒绝；连接池把物理会话复用于多个逻辑请求。
- **解决方案**：所有租户表启用并强制 PostgreSQL RLS；每个事务使用参数化 `set_config(..., true)` 写入事务级租户上下文；平台迁移使用独立角色。
- **验证方式**：架构测试枚举租户表并检查 Enable、Policy、Force 三项；Python 适配器拒绝非 UUID 输入并只生成参数化调用。
- **预防措施**：数据访问必须在显式事务中进行；禁止使用会话级 `SET`；新增租户表时架构测试会要求同步增加策略。
- **遗留风险**：当前开发机没有 PostgreSQL 容器，真实连接池的跨事务集成测试尚未执行，因此状态为“观察中”。

## NX-010 · Studio 无法从内部运行状态稳定构造可解释视图

- **首次发现**：2026-08-22
- **来源**：场景验证
- **现象**：用现有 API 设计运行详情页时，只能获得最终任务状态和选中 Skill ID，无法稳定展示任务标题、依赖关系、路由候选分、排除原因和事件时间线。
- **影响**：界面如果直接读取内部 `NexusState`，会与 Python 实现细节耦合；如果自行拼接数据，又会让 UI 展示与真实决策不一致。
- **定位过程**：逐项对照 Studio 信息架构和 `/v1/prd/runs` 响应，确认问题是缺少面向查询的读模型，而不是单个组件渲染错误。
- **根因**：早期 API 只为同步演示返回最终产物，没有为可观测控制台设计摘要、详情和事件投影。
- **解决方案**：先用独立 TypeScript View Model 和明确标记的演示快照冻结页面需求；随后增加运行摘要、详情和路由/预算投影 API，不导入 Python 内部类型，也不允许 Studio 直读数据库。
- **验证方式**：投影契约测试覆盖任务定义、路由原因、时间和产物；内存存储验证容量淘汰与防御性副本；页面仍显示“演示快照”，样例指标不进入正式监控结论。
- **预防措施**：Studio 新字段必须先进入版本化 API 投影；禁止通过数据库表或内部状态对象绕过服务契约。
- **遗留风险**：Studio 尚未切换到真实 API，进程内投影重启后会丢失；完成前保持“观察中”，并需要补加载、空状态和失败状态测试。

## NX-011 · 外部 Agent 的重复或乱序事件会让任务状态回退

- **首次发现**：2026-08-23
- **来源**：集成测试、故障推演
- **现象**：远程 Agent 完成后，延迟到达的 `working` 事件可能覆盖本地完成态；重复 `completed` 事件又可能让同一产物被消费两次。
- **影响**：Nexus Run 长期显示运行中、重复触发下游任务，或者在已经交付产物后再次计费。
- **定位过程**：按“完成事件先到、旧工作事件后到”和“同一状态重复投递”两组顺序驱动生命周期，确认直接赋值状态无法保持单调性。
- **根因**：外部状态被当成普通字段同步，没有合法迁移表、终态保护和重复事件语义。
- **解决方案**：增加 A2A Task 状态机；只允许显式迁移，四种终态不可回退；相同状态重复事件返回当前投影；产物只能随完成态写入，失败必须携带稳定错误码。
- **验证方式**：自动化测试覆盖完成、等待输入后恢复、终态回退、重复提交、失败错误码和非 HTTPS Agent Card 六类路径。
- **预防措施**：远程适配器不得直接修改 Nexus Task；新协议状态必须先定义映射和迁移测试。
- **遗留风险**：当前未连接真实 A2A Server，跨连接事件序号和多副本并发仍需持久化版本号或条件写保证。

## NX-012 · 首次统一 Lint 暴露跨模块风格漂移

- **首次发现**：2026-08-23
- **来源**：开发测试
- **现象**：新增 CI 后在本地执行同一 Ruff 命令，发现 10 个文件需要格式化、31 个导入顺序、现代化语法、行长或框架规则问题。
- **影响**：直接提交工作流会让 Python Job 必然失败；不同阶段代码风格继续漂移也会增加评审噪声。
- **定位过程**：先运行只读的 `ruff format --check` 和 `ruff check`，区分安全自动修复项、纯格式项与需要人工判断的问题。
- **根因**：项目前期只执行功能测试，虽然 `pyproject.toml` 已声明 Ruff 规则，但没有在每次提交或统一入口中实际执行。
- **解决方案**：使用同一 Ruff 版本完成安全修复和格式化；手工处理 FastAPI Body 推断、类型联合、路由条件简化与长字符串；CI 固定执行格式和 Lint 检查。
- **验证方式**：`python -m ruff format --check .` 与 `python -m ruff check .` 均零错误，完整 Python 测试随后再次执行。
- **预防措施**：Python Job 把格式和 Lint 置于测试之前；开发者提交前使用与 CI 相同命令。
- **遗留风险**：当前没有 pre-commit Hook，提交者仍可能到远程 CI 才看到问题，但主分支质量门可以阻止合入。

## NX-013 · Studio 静默使用样例时容易被误认为真实监控

- **首次发现**：2026-08-29
- **来源**：场景验证
- **现象**：API 没有启动时，控制台仍展示完整运行、成功率和 Token 指标，但页面无法说明这些数字来自演示快照。
- **影响**：使用者可能据此判断系统健康或效果，掩盖真实 API 故障，并把样例数据带入演示结论。
- **定位过程**：分别在未配置地址、地址拒绝连接和返回非 2xx 三种情况下检查总览与详情，确认数据本身正常但来源不可见。
- **根因**：首版快照只用于冻结信息架构，没有把数据来源作为页面状态建模。
- **解决方案**：建立 API 优先的数据源适配器，设置 2.5 秒超时；失败时返回快照和错误说明；总览与详情始终显示“实时 API”或“演示快照”，快照模式额外展示黄色告警。
- **验证方式**：未设置环境变量时确定性进入快照模式；所有网络和状态码异常通过同一降级分支返回可见告警；类型适配对未知状态按失败处理。
- **预防措施**：样例、缓存和实时数据必须携带来源；写操作在非实时来源下保持禁用。
- **遗留风险**：当前尚未完成 Node.js 生产构建验证，CI 通过前仍可能存在前端类型或构建兼容问题。

## NX-014 · 长任务在全局上下文充足时仍被分区硬上限拒绝

- **首次发现**：2026-08-29
- **来源**：场景验证、开发测试
- **现象**：Task 分区默认只有总窗口的 8%，当用户原始需求超过该比例时，即使其他分区完全空闲且总输入预算充足，运行仍抛出 `required context does not fit budget`。
- **影响**：详细的企业需求无法进入 Planner/Agent，用户只能人为删减关键约束；提高总窗口也可能仍被固定比例阻断。
- **定位过程**：用 100 Token 总窗口、20 Token 输出预留和约 15 Token 必要 Task 构造最小用例；全局可用 80 Token，但原算法因 Task 上限 8 Token 拒绝。
- **根因**：分类占比同时承担了防挤占和必要内容可用性两种冲突职责，被错误实现为所有片段的硬上限。
- **解决方案**：总输入预算继续作为硬上限；必要片段允许借用未使用分类空间，普通片段仍按分类上限裁剪；报告记录 `borrowed_tokens`。
- **验证方式**：回归测试确认 15 Token 必要 Task 被保留、借用 7 Token、同分区普通内容被裁剪，且总输入不超过 80 Token；真正超过全局预算的必要内容仍失败。
- **预防措施**：修改分区比例时同时覆盖“分区超限但全局可容纳”和“全局不可容纳”两类测试，并监控借用量。
- **遗留风险**：多个必要片段仍按优先级依次占用全局预算；未来需要在入口阶段给出更友好的拆分建议，而不只是抛出错误。

## NX-015 · 模型供应商瞬时故障直接终止整条 Agent 任务

- **首次发现**：2026-08-30
- **来源**：故障推演、开发测试
- **现象**：主模型端点超时、返回 429 或 5xx 时，调用异常直接传播到 Runtime，即使已经配置另一个健康模型也不会接管。
- **影响**：单一供应商抖动使整条多 Agent 运行失败；人工重试会重复前序工作和 Token 消耗。
- **定位过程**：用记录请求的假后端分别注入瞬时不可用和永久请求错误，确认原 Gateway 没有错误分类或后端顺序策略。
- **根因**：统一模型契约只抽象了调用格式，没有抽象目标、数据放置限制和可降级错误语义。
- **解决方案**：增加有序 `ModelTarget` 和 `FallbackModelGateway`；只对瞬时错误切换，每目标最多一次；永久错误立即失败；根据请求数据分类过滤目标。
- **验证方式**：测试覆盖主端限流后切换、永久拒绝不切换、受限数据跳过云端、所有目标故障聚合四条路径，并检查每个后端调用次数。
- **预防措施**：新增适配器必须把供应商错误映射为瞬时不可用或永久拒绝；降级目标需显式声明数据分类。
- **遗留风险**：超时不代表供应商一定未执行，切换可能重复计费；真实供应商遥测和费用对账完成前保持“观察中”。

## NX-016 · Memory 全局召回可能混入其他租户或过期内容

- **首次发现**：2026-08-30
- **来源**：安全测试、场景验证
- **现象**：两个租户写入包含相同关键词的历史结果后，原内存实现按全部 Run 全局排序，查询方可能得到另一个租户的文本；过期和受限内容也没有过滤条件。
- **影响**：造成跨租户数据泄露、陈旧结论污染新任务，受限数据还可能被发送给不合规模型或工具。
- **定位过程**：为两个租户写入相似文本，再以同一查询检索；检查实现发现存储键只有 `run_id`，条目没有租户、分类、来源和有效期。
- **根因**：早期 Memory 只用于相关度演示，把文本相似度当成唯一检索条件，没有把授权与保留策略纳入端口。
- **解决方案**：Memory 条目增加租户、来源、分类和可选过期时间；读写端口强制传递租户；先过滤租户、允许分类和有效期，再计算词法相关度。
- **验证方式**：回归用例同时写入跨租户、Public/Internal、Restricted 和过期内容；普通查询只返回本租户有效的 Public/Internal，显式授权后可返回本租户 Restricted，始终不能返回其他租户内容。
- **预防措施**：生产向量库必须使用服务端过滤或租户分区，禁止先全局召回再由 Agent 判断权限；API 租户只能来自可信身份。
- **遗留风险**：当前 InMemory 实现未对来源可信度做排序，恶意但同租户的历史内容仍可能形成 Memory Poisoning，需要后续增加来源信任与用户撤销机制。

## NX-017 · 单一健康端点无法区分进程存活与接流准备

- **首次发现**：2026-09-05
- **来源**：部署验证、故障推演
- **现象**：`/healthz` 只要 HTTP 进程可响应就返回 200，即使 Agent/Skill 清单缺失或关键依赖不可用，Compose/负载均衡仍认为实例健康。
- **影响**：故障实例继续接收新 Run；如果同一端点又被用作 Liveness，依赖短时故障会触发无意义重启并放大恢复时间。
- **定位过程**：分别模拟“进程正常但关键检查失败”和“仅可选遥测失败”，确认单一布尔状态无法支持正确的摘流、降级与重启决策。
- **根因**：早期健康端点只服务开发启动检查，没有建立 Liveness、Readiness 和可选依赖降级语义。
- **解决方案**：`/livez` 只证明进程可响应；`/readyz` 并发执行每项有界检查，关键失败返回 503，可选失败返回 200 + `degraded`；旧端点保留兼容但标记弃用。
- **验证方式**：测试覆盖全通过、可选失败、关键失败、超时与异常脱敏；Compose 改用 `/readyz` 作为健康检查。
- **预防措施**：编排平台不得用 Readiness 触发进程重启，也不得用 Liveness 决定是否接收流量；新依赖必须声明是否关键。
- **遗留风险**：当前默认只检查本地 Agent/Skill 清单，真实数据库、消息和模型检查要在对应适配器可用后注册，并避免探针雪崩。

## NX-018 · API 已安装但测试客户端缺少传输依赖

- **首次发现**：2026-09-05
- **来源**：开发测试
- **现象**：安装 `.[api]` 后服务代码可以导入，但 `fastapi.testclient.TestClient` 在收集测试时抛出 RuntimeError，提示还需要 `httpx2`。
- **影响**：现有 CI 使用 `.[dev,api]` 仍会在 HTTP 集成测试导入阶段失败，健康探针和真实路由得不到验证。
- **定位过程**：确认 FastAPI、Starlette 与 Uvicorn 已安装，再沿异常栈定位到 Starlette TestClient 的独立传输依赖，而不是应用路由异常。
- **根因**：`api` extra 只包含服务运行依赖，`dev` extra 没有声明当前 TestClient 所需的测试传输包。
- **解决方案**：把 `httpx2>=2.12` 加入开发依赖，并在本机安装与 CI 相同的 `dev + api` 组合。
- **验证方式**：TestClient 成功覆盖 `/livez`、关键依赖失败的 `/readyz`、PRD 创建、运行列表和详情，共 8 项健康与 HTTP 集成测试通过。
- **预防措施**：可选运行依赖对应的测试依赖必须进入同一个 CI 安装组合；不能只用模块导入证明 HTTP 层可用。
- **遗留风险**：依赖仍使用下限范围，未来上游主版本变化需要由 CI 及时暴露，并通过锁文件或上限约束处理。

## NX-019 · CLI 从仓库外启动时无法定位 Agent 与 Skill 清单

- **首次发现**：2026-09-05
- **来源**：实际使用、开发测试
- **现象**：Editable Install 后在其他工作目录执行 `nexus prd`，默认从当前目录寻找 `agents/` 和 `skills/`，最终表现为无法解析 Agent 或无可用 Skill。
- **影响**：用户容易把资产路径错误判断为路由失败；生产示例凭据、缺少 API 或多语言命令也要到使用对应功能时才发现。
- **定位过程**：比较仓库根目录与空目录运行路径，确认 Python 包可以导入，但版本化清单没有打入 Wheel 且 Composition Root 使用相对路径。
- **根因**：CLI 没有显式资源根目录，项目也缺少统一前置诊断；仓库资产和 Python 包的分发边界未向用户暴露。
- **解决方案**：`prd` 和 `doctor` 增加 `--root`；诊断命令实际加载 Agent/Skill、校验安全配置，并报告可选模块与外部命令；关键失败和可选降级使用不同退出语义。
- **验证方式**：测试确认正确根目录在缺少可选工具时返回 `degraded`/退出码 0，严格模式返回 1；错误根目录和无效生产设置返回 `failed`，且报告不泄露示例密钥。
- **预防措施**：快速开始先运行 `nexus doctor --root .`；部署入口使用固定工作目录；远程 Registry 落地前不隐藏仓库资产依赖。
- **遗留风险**：Python Wheel 仍不能独立携带 Agent/Skill，离线分发需要同时交付仓库资产或未来导出的 Registry Bundle。

## NX-020 · Windows 终端中的中文诊断结果显示乱码

- **首次发现**：2026-09-05
- **来源**：实际使用
- **现象**：`nexus doctor --json` 结构和退出码正确，但 PowerShell 中的中文 `detail` 与 `remediation` 字段显示为乱码。
- **影响**：使用者无法直接阅读诊断和处理建议，复制后的 JSON 也可能被错误编码污染。
- **定位过程**：同一报告在 Python 内部断言为正确 Unicode，只有进程标准输出后损坏；确认 `ensure_ascii=False` 输出中文，而 Windows 标准输出编码与调用端解码不一致。
- **根因**：CLI 依赖宿主终端的默认编码，没有为跨平台中文输出固定 UTF-8。
- **解决方案**：CLI 启动时若标准输出支持 `reconfigure`，显式设置 UTF-8 和替换式错误处理，再执行文本或 JSON 输出。
- **验证方式**：在同一 PowerShell 环境重新执行 `python -m nexusos.cli doctor --root . --json`，中文状态、处理建议和 JSON 结构均可读，退出码仍保持原语义。
- **预防措施**：所有 CLI 入口复用同一输出初始化；机读 JSON 始终以 UTF-8 作为传输编码。
- **遗留风险**：不支持 `reconfigure` 的嵌入式 Stream 将保持宿主编码，但代码会跳过设置而不影响核心命令。

## NX-021 · Mypy 虽已列为依赖但从未进入质量门

- **首次发现**：2026-09-06
- **来源**：开发测试
- **现象**：发布前首次对核心包、基准和测试运行 Mypy，共发现 9 个文件中的 27 个错误；此前 CI 安装了 Mypy，却没有实际执行命令。
- **影响**：动态 Settings 构造、过宽的基准报告类型和未声明的测试类属性会掩盖接口变化，消费者可以对 `object` 做不安全索引而不被提前阻止。
- **定位过程**：对 `packages/nexusos-python/src/nexusos`、`benchmarks` 和 `tests` 同时检查，把问题分为第三方 Stub 缺失、动态构造推断失败、报告返回类型过宽和测试夹具类型缺失四类。
- **根因**：开发依赖和 CI 步骤没有对应关系；早期只依靠运行时测试与 Ruff，未把静态类型作为合并门。
- **解决方案**：Settings 改为显式字段构造；路由与规模报告使用 `TypedDict`；测试类属性使用 `ClassVar`；加入 `types-PyYAML`；CI 真正执行 Mypy。
- **验证方式**：同一命令检查 81 个源文件并输出 `Success: no issues found`，随后再运行完整测试套件。
- **预防措施**：新增开发工具时必须同时增加可执行 CI 步骤；公开字典结果必须提供结构类型或数据类。
- **遗留风险**：当前 Mypy 采用默认严格度，尚未开启 `strict = true`；后续按模块收紧，避免一次性产生无法评审的大量噪声。

## NX-022 · 许可证正文因人工拷贝出现缺词和重复词

- **首次发现**：2026-09-09
- **来源**：发布封版审计
- **现象**：Apache License 2.0 的定义段落中缺少 `is` 和括号说明，并出现 `Lic Licensor` 重复词；包元数据虽然仍声明 Apache-2.0，但仓库内正文并非完整标准文本。
- **影响**：使用者和自动许可证扫描器可能对授权正文的完整性产生疑问，发布物的合规证据不可靠。
- **定位过程**：在构建发布包前逐段审阅 `LICENSE`，再与 Apache Software Foundation 发布的官方 2.0 文本按行比对；确认差异来自正文损坏，而非允许替换的版权持有人占位符。
- **根因**：初始化仓库时手工复制和调整许可证文本，没有在 CI 中校验标准许可证正文，只检查了包元数据中的 SPDX 标识。
- **解决方案**：恢复官方定义段落的完整原文，保留附录中允许替换的 `Copyright 2026 NexusOS Contributors` 版权声明。
- **验证方式**：将官方文本的版权占位符替换为项目声明后再次逐行比较，除文件首尾空行外不再存在正文差异。
- **预防措施**：许可证升级或版权声明调整必须以官方原文为基线，仅允许修改附录中的占位字段；发布审计继续执行逐行比对。
- **遗留风险**：该检查尚未自动化；后续可以保存官方文本摘要或引入 REUSE/ScanCode，但不得依赖易变化的网络请求作为普通离线测试前置条件。

## NX-023 · Windows 工作区中新写入文件触发 Ruff 读取异常

- **首次发现**：2026-09-09
- **来源**：发布封版审计、开发测试
- **现象**：当前 Windows 桌面工作区中，Ruff 直接读取刚由补丁工具重建的纯 ASCII Python 文件时报告 `stream did not contain valid UTF-8`；Python `read_bytes`/UTF-8 解码和 Ruff 标准输入模式读取同一内容均正常，升级 Ruff 后现象仍存在。
- **影响**：本地全仓格式和 Lint 命令被文件系统读取异常中断，容易把工作区互操作问题误判为源码编码损坏。
- **定位过程**：检查原始十六进制字节、最大字节值、NTFS 数据流和 Python UTF-8 解码；复制文件后异常随文件实体复现，而通过标准输入交给同一 Ruff 版本则通过，因此排除源码中的非法 UTF-8 字节和语法错误。
- **根因**：问题位于当前 Windows 宿主机的文件过滤或原生读取互操作层，不在 Python 源码内容；同一宿主机新建的无硬链接 Git 检出也会复现。曾怀疑由加密功能导致，但临时关闭加密、重建文件并由 `cipher` 确认为未加密后仍可复现，因此加密不是充分根因。
- **解决方案**：发布审计对受影响文件使用 `--stdin-filename` 检查源码内容；正常 CI 应使用不带该宿主机加密/过滤条件的独立工作区执行标准 Ruff 命令。
- **验证方式**：受影响文件通过 Python 严格 UTF-8 解码和 Ruff 标准输入检查；修改前的其余源码已通过标准全仓 Ruff，73 项测试、Mypy、文档和 Wheel 构建继续通过。
- **预防措施**：遇到 E902 时先比较 Git Blob、磁盘字节和标准输入结果，不要盲目修改编码声明或业务代码；将外部 CI 或迁移后 Linux 工作区的标准 Ruff 日志作为彻底关闭该问题的条件。
- **遗留风险**：当前 Windows 宿主机上的直接全仓 Ruff 仍会产生环境性假失败，因此状态保持“已缓解”；源码发布不受阻。迁移到 Linux 后必须取消 `--exclude` 和标准输入绕过，执行标准全仓命令，通过后才能更新为“已解决”。

  **2026-09-09 后续处理**：临时关闭宿主机加密功能并重新创建包入口文件，`cipher /c` 返回 `U`，标准全仓 Ruff 仍只在该新文件上失败；复制到未加密临时目录后现象不变。由此撤回“关闭加密即可恢复”的初步判断，继续采用内容级检查并等待独立 CI 证据。

## NX-024 · 无隔离 Wheel 构建因本机缺少构建后端而失败

- **首次发现**：2026-09-09
- **来源**：发布封版审计、实际使用
- **现象**：执行带 `--no-build-isolation` 的 `pip wheel` 时，在元数据阶段报错 `Cannot import 'setuptools.build_meta'`；同一解释器的用户环境没有安装 Setuptools 和 Wheel。
- **影响**：如果发布脚本错误地假设本机已有构建后端，源码测试全部通过后仍无法生成分发包。
- **定位过程**：直接导入 `setuptools.build_meta` 并查询 `pip show setuptools wheel`，确认后端不在当前环境；项目的 `[build-system]` 声明存在，标准隔离构建能够自行安装所需后端。
- **根因**：审计命令主动关闭了 PEP 517 构建隔离，却没有先建立包含构建依赖的受控环境；这不是项目源码或包元数据解析失败。
- **解决方案**：发布流程恢复标准隔离构建，并把采用 SPDX License 表达式所需的最低 Setuptools 版本明确为 77.0.3，同时把 `LICENSE` 纳入 Wheel。
- **验证方式**：`python -m pip wheel --no-deps --wheel-dir <临时目录> .` 成功生成 `nexusos-0.1.0-py3-none-any.whl`，元数据版本和包内许可证均正确。
- **预防措施**：只有离线、预装并锁定构建后端的专用环境才允许使用 `--no-build-isolation`；普通开发和发布文档使用 PEP 517 默认隔离行为。
- **遗留风险**：隔离构建首次运行需要访问依赖源；正式离线发布环境应预先缓存由锁定摘要校验的 Setuptools 和 Wheel。

## 新问题记录模板

复制下面内容并按首次发现日期追加，编号不可复用：

```markdown
## NX-XXX · 简短问题标题

- **首次发现**：YYYY-MM-DD
- **来源**：开发测试 / 场景验证 / 基准测试 / 部署运行
- **现象**：可以观察或复现的行为
- **影响**：对用户、数据、成本、稳定性或研发效率的影响
- **定位过程**：关键证据和排除项
- **根因**：最终确认的技术或流程原因
- **解决方案**：实际实施的修复或缓解措施
- **验证方式**：测试、指标、查询或复现步骤
- **预防措施**：如何防止同类问题再次发生
- **遗留风险**：尚未覆盖的边界；没有则写“无已知遗留风险”
```

## 2026-09-10：PRD 核心仍为演示交互

- 现象：任意主题生成同一记账内容；保存和 78 分完整度无真实依据，工作台输入未传给编辑页。
- 根因：页面使用定时器与硬编码正文；默认后端为离线 Runtime，真实模型适配器未形成产品入口；缺少持久化写作服务。
- 解决：新增文档、简报、版本、任务持久化；接入真实模型多阶段写作与独立评审；编辑保存、恢复、反馈修订和 MD/HTML 导出；保留原始材料；取消固定评分和无效入口。
- 验证：新增 PRD 工作区 HTTP 集成测试覆盖生成、上下文和来源透传、路由、修订、保存冲突、停止、重启、评审失败、无模型与截断；前端类型检查与生产构建通过。
- 遗留：需要使用者指定并配置真实模型，才能验证真实内容质量与费用；不将受控模型测试算作模型质量验收。工作区仅支持单 API 实例和个人环境，尚无用户认证。

## 2026-09-10：模型非法响应被当成正文

- 现象：兼容服务返回 `content: null` 时旧适配器会转换成字符串 `None`；部分无效用量和结构错误不在错误分类边界内。
- 根因：只对 HTTP/JSON 读取做校验，后续内容强制转字符串；Runtime 未检查截断状态。
- 解决：适配器要求非空字符串正文、非负合法用量，异常统一分类；Runtime 对空、截断、过滤等非正常终止显式失败。
- 验证：用空值、空白、对象正文、非法用量及截断响应覆盖回归路径。
- 遗留：供应商参数与计费口径不同，需要使用实际服务集成验证；当前统计不是完整供应商账单。

## 2026-09-10：开发服务与生产验证构建互相覆盖

- 现象：同目录运行 Next 开发服务和生产验证时，开发服务覆盖 `.next`，已启动的验证服务报 `required-server-files.json` 缺失。
- 根因：两种服务共用构建输出目录。
- 解决：支持 `NEXUS_STUDIO_DIST_DIR`，验证构建使用 `.next-preview` 并运行独立 standalone 服务；正式默认目录保持 `.next`。生成目录和 TypeScript 增量缓存不纳入源码。
- 验证：独立目录完成生产构建，standalone 服务启动成功。

## 2026-09-10：写作工作区浏览器与模型边界验收

- 真实 Chromium 操作已验证：工作台输入传递、服务器保存/重开、Markdown 表格预览、HTML 导出与脚本不执行、浏览器草稿恢复、历史版本恢复、并发编辑冲突、390px 移动布局、NexusOS 预填简报和模型未配置提示；页面无脚本错误。
- 正式兼容模型适配器已对本机 HTTP 测试服务完成五阶段调用，验证请求路径、认证头、所选模型、输出上限、输入材料与供应商返回用量。该测试仅验证协议与系统行为，不代表真实大模型内容质量。
- 浏览器生成链路检查发现新任务执行期间仍可能显示上一次评审，已将旧评审隐藏到当前任务完成后，避免误当成新稿结论。
- NexusOS 自身 PRD 已保存到当前本机文档库，同时保留仓库 Markdown 源稿。草稿由本次开发协作整理，未冒充模型生成或已批准文档。

## 2026-09-10：PRD 任务与产物缺少完整执行证据

- 发现：检查已提交工作区 `b65afcc` 的任务创建、执行、模型错误与版本保存，确认输入未固定快照、失败响应未保留、版本未绑定来源任务、UI 只能便捷查看最后任务。
- 方案：输入和产物血缘、只追加持久事件、模型边界包装、W3C 标识传播、历史任务分页与导出；显式重新执行关联旧任务。
- 验证：集成测试覆盖消息和标识关联、原始快照稳定、截断保留、失败/取消/重启、重试血缘、原子保存、禁止应用更新历史、审计失败不发模型请求、旧记录缺失和分页；本机 HTTP 服务核对传播头与供应商请求 ID。
- 详细过程与技术嵌入：[RT-001 全程追溯](runtime-issues/001-end-to-end-tracing.md)。
- 后续关键问题不混写为已解决：[11 项运行问题台账](critical-runtime-backlog.md)。


## 2026-09-10：生成失败后无法复用已完成阶段

- 现象：实际追溯显示一条任务三步分析成功后写作截断，另一条任务首步分析就截断；旧界面只能重新发起，无法保存执行现场并继续。
- 根因：展示与追溯结果尚未形成可恢复阶段状态，Runtime 没有持久 checkpointer；两个任务的输出上限失败需要分别排查。
- 方案：LangGraph AsyncSqliteSaver、事务内业务阶段检查点、原始输入与修订校验、发布去重，以及重新执行/继续执行两种操作。
- 验证：API 测试覆盖重开、连续失败、取消、旧任务证据导入、模型成功后业务提交失败；独立子进程 SIGKILL 后继续，确认已完成阶段不重复请求。
- 遗留：未知远端结果可能重调；没有自动恢复调度、多实例租约和工具 exactly-once。详见 [故障排查](trouble-shooting.md) 与 [RT-003](runtime-issues/003-checkpoint-recovery.md)。


## 2026-09-10：恢复操作位置分散、按钮过大

- 反馈：重新执行与继续执行位于独立侧栏卡片，通用按钮及展开说明占用过多空间，手机上入口随侧栏落到正文之后。
- 调整：移动至主编辑区顶部的任务状态条；追溯页放入所选任务摘要。同一视图只保留一组操作，说明默认折叠，不可继续原因仍直接显示。
- 验证：Studio 生产构建及 Chromium 桌面/390px 布局通过；按钮约 70px 宽，桌面高 30px、手机高 36px，无横向溢出，需求变更后的禁用逻辑仍正常。
