# PRD 质量评估

PRD 评估采用清晰度、完整性、可行性、一致性和证据覆盖五个维度，总体分达到 85 且没有阻断问题才通过。

第一层使用确定性结构规则，确保空文档、核心章节缺失和未标记事实不会被模型评审掩盖。第二层后续增加模型评审与人工抽样，但模型分数不能覆盖确定性阻断规则。

评审输出必须包含维度分、阻断问题与可执行修订任务。Replanner 根据具体修订任务更新 DAG，禁止只用“继续改进”作为反思结果。

Rubric 位于 `benchmarks/prd/rubric.yaml`，任何阈值或维度变更必须版本化。
