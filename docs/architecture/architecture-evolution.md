# 总体架构方案与演进记录

本文是项目最初的总体方案，也是架构变更的唯一连续记录。每次调整系统边界、核心抽象、关键技术选型或部署模型时，都必须在本文末尾追加带日期的迭代记录；历史结论不删除，只标记其后续状态。

## 0.1 初始方案

### 问题定义

系统需要在上万个 Skill 和多个 Agent 的环境中，以更少 Token、更低延迟和更高准确率完成复杂任务。

### 初始结构

```text
用户请求
  -> 统一编排服务
  -> 意图识别与任务拆解
  -> Skill 元数据检索
  -> Agent 执行
  -> 结果评审
  -> PRD 产物
```

第一版暂定使用单一 Python 服务承载 API、编排、Skill 检索和执行，以最短路径验证业务闭环。外部存储与模型通过适配器访问，避免核心逻辑绑定供应商。

### 初始原则

- 只常驻轻量 Skill 元数据，完整指令按需加载。
- Agent、Skill 与 Tool 是不同资源，不在模型层混为一体。
- 先形成可运行闭环，再拆分分布式服务。
- 每次执行必须记录路由、Token、延迟和评审结果。

## 迭代记录

### 2026-05-02 · v0.1 · 建立单体参考架构

- 修改内容：建立单一 Python 编排服务、Skill 元数据目录和 PRD 生成参考场景的初始边界。
- 修改原因：项目尚处于问题验证期，先减少基础设施变量，验证能力检索是否能降低上下文开销。
- 影响范围：仅形成文档与工程约束，尚未冻结运行时、存储和协议实现。
- 后续验证：以 100、1,000、10,000 个 Skill 的路由实验比较 Token、召回率和延迟。

### 2026-05-05 · v0.2 · 项目更名为 NexusOS（纽带）

- 修改内容：将工作名称 AgentOS-Lab 更改为 NexusOS，中文名确定为“纽带”，并确立独立文档域名 `nexusos.net.cn`。
- 修改原因：旧名称容易被理解为教学实验；新名称更准确地表达连接 Agent、Skill、Tool、Memory、Knowledge 与人的基础设施定位。
- 影响范围：项目标识、文档站配置、公开描述和后续模块命名统一使用 Nexus 前缀。
- 兼容说明：本次仅改变品牌与表述，不改变 v0.1 的验证边界。

### 2026-05-09 · v0.3 · 采用 Contract First 与六边形内核

- 修改内容：把“单一 Python 服务”细化为模块化单体；领域层只依赖 Agent Runtime、Skill Repository、Model Gateway、Memory Store 等端口，供应商 SDK 位于适配器层。
- 修改原因：如果业务逻辑直接依赖 LangGraph、模型厂商或向量数据库，后续无法做运行时对照实验，也难以演进到多语言服务。
- 影响范围：后续代码必须从稳定契约开始，默认实现可以留在进程内，但不能让基础设施类型泄漏到领域模型。
- 取舍：模块化单体增加少量接口代码，换取可测试性、替换能力和后续服务拆分路径。

### 2026-05-14 · v0.4 · 冻结第一组领域契约

- 修改内容：将 Goal、Task、TaskGraph、NexusState、AgentRuntime、SkillRepository、MemoryStore、ToolGateway 和 RunRepository 落为框架无关的 Python 契约。
- 修改原因：在引入工作流框架与存储前先验证依赖方向，避免实现类型泄漏到领域模型。
- 影响范围：任务图必须是有向无环图；所有运行时统一返回 AgentResult；运行状态可由任意适配器持久化。
- 兼容策略：契约仍处于 `0.x`，允许通过新增可选字段演进，破坏性变更必须补充 ADR。

### 2026-05-24 · v0.5 · 固化 Skill 渐进加载边界

- 修改内容：把 Skill 拆分为常驻索引的 `SkillSummary` 与命中后读取的 `SkillPackage`，统一使用版本化 Manifest 描述领域、能力、关键词、工具依赖、成本、风险和历史质量。
- 修改原因：完整 Skill 指令和示例随目录规模线性进入上下文会造成 Token 爆炸，检索阶段只需要少量可排序元数据。
- 影响范围：Registry 扫描时禁止读取指令正文；Executor 只能在 Router 完成策略过滤和选择后加载完整包。
- 取舍：Manifest 维护成本增加，但获得了可索引、可验证、可版本化和可审计的能力目录。

### 2026-06-03 · v0.6 · 从单一相似度升级为多信号路由

- 修改内容：将初始“Skill 元数据检索”细化为领域识别、语义与词法相关度、能力匹配、历史成功率、成本和延迟的加权排序，并在选择前执行工具、风险与领域硬约束。
- 修改原因：单一向量相似度不能表达授权、成本和可靠性，容易选出语义相关但不可执行或性价比过低的 Skill。
- 影响范围：每个候选必须携带可解释评分分量；Token 预算成为 Router 的输入；生产向量检索仅替换召回信号，不绕过策略层。
- 当前限制：离线实现使用确定性词法近似，后续通过 Qdrant 适配器引入真正的稠密与稀疏混合召回。

### 2026-06-12 · v0.7 · 增加显式上下文预算

- 修改内容：在 Runtime 前增加 Context Budget Manager，将系统、任务、Skill、Memory、检索证据、工具 Schema 和输出预留分别设定预算。
- 修改原因：路由只解决“选哪些能力”，不能阻止历史消息、检索结果和工具描述继续挤占上下文。
- 影响范围：Agent Runtime 只能接收已经构建的 `AgentContext`；必要上下文不能静默截断；所有省略项与分区用量进入预算报告。
- 取舍：离线 Token 估算只保证保守和确定性，生产环境由模型适配器提供准确 tokenizer。

### 2026-06-14 · v0.8 · 将 Memory 按生命周期分层

- 修改内容：Memory 明确拆分为 Working、Episodic 与 Semantic 三类，并通过统一 `MemoryStore` 端口向 Context Builder 提供候选片段。
- 修改原因：当前任务状态、历史运行经验和长期知识具有不同的一致性、查询与保留要求，使用单一聊天记录无法可靠治理。
- 影响范围：本地开发使用支持中英文检索的内存适配器；生产规划分别映射到 Redis、PostgreSQL 和 Qdrant，但领域层不感知产品类型。
- 隐私约束：跨运行检索后续必须增加租户、用户和数据分类过滤，不能把全局相似度作为授权依据。

### 2026-06-20 · v0.9 · Agent 从固定角色改为声明式发现

- 修改内容：新增版本化 Agent Registry 与基于任务能力、领域的 Agent Resolver；工作流不再直接写死角色实现。
- 修改原因：固定 `PM -> UX -> Tech -> Writer` 链条无法根据任务复杂度增减专家，也会把角色、Skill 和 Tool 权限混在一起。
- 影响范围：Planner 只声明任务所需能力；Resolver 选择 Agent；随后 Router 必须在 Agent 的 Skill 与 Tool 策略内选能力。
- 安全收益：Agent 的工具白名单由 Manifest 声明，后续 Policy 层可以在执行前进行独立复核。

### 2026-06-21 · v0.10 · PRD 流程从固定顺序改为能力导向 DAG

- 修改内容：Planner 生成只声明依赖与所需能力的任务 DAG；标准需求并行执行 UX 与技术评估，复杂需求额外并行行业研究。
- 修改原因：固定 Agent 链既浪费并行机会，又把规划结果与当前角色目录绑定，无法验证动态组队。
- 影响范围：Agent Resolver 成为 Planner 与 Runtime 之间的必经步骤；运行时按拓扑层调度；工作流必须先通过 DAG 不变量校验。
- 应用边界：PRD 任务模板位于 Reference Application，通用核心只保留 Goal、Task 与 TaskGraph。

### 2026-06-21 · v0.11 · 建立可离线验证的进程内 Runtime

- 修改内容：把 Planner、Agent Resolver、Skill Router、Context Builder、Memory 与 `AgentRuntime` 端口串成完整执行闭环，并按 DAG 拓扑层并发执行。
- 修改原因：只有可重复的端到端运行才能暴露模块边界问题；早期直接依赖在线模型会让测试受网络、费用和模型漂移影响。
- 影响范围：新增确定性 Local Runtime 作为测试适配器；任务级记录 Agent、Skill、预算、Token、产物和生命周期事件；失败任务停止当前运行。
- 后续替换：Local Runtime 不作为生产推理方案，下一阶段在同一端口后接 LangGraph 和模型网关。

### 2026-07-04 · v0.12 · 固定传输层与应用服务边界

- 修改内容：增加 FastAPI 工厂与 `/v1/prd/runs` v1 传输契约，使用独立 Bootstrap 组合应用依赖；API 只做参数校验和序列化。
- 修改原因：如果 HTTP 类型进入编排器，CLI、Worker 和测试将无法复用同一应用服务，也会让未来从同步请求迁移到异步运行变得困难。
- 影响范围：核心包不强制依赖 FastAPI；所有入口通过 Composition Root 构建端口实现；v1 响应保留任务选择、评审、Token 和产物。
- 演进路径：当前接口同步返回完成结果，分布式 Runtime 上线后增加状态查询，同时保持已公开字段的兼容性。

### 2026-07-11 · v0.13 · 模型调用收敛到统一 Gateway

- 修改内容：新增与供应商无关的消息、请求、响应与 Token 用量契约，并提供离线确定性实现和 OpenAI-compatible HTTP 适配器。
- 修改原因：Agent 直接持有供应商 SDK 会让模型切换、成本统计、降级和数据策略分散在各个节点，无法统一治理。
- 影响范围：Runtime 只依赖 `ModelGateway.complete`；供应商模型名、鉴权和响应格式由适配器处理；Token 用量必须映射到核心模型。
- 后续能力：模型选择、fallback、限流和内容策略将在 Gateway 上层独立演进。

### 2026-07-12 · v0.14 · 明确 LangGraph 与 Temporal 的分层职责

- 修改内容：`AgentRuntime` 增加可选 LangGraph 实现；LangGraph 管理单个 Agent 的推理图，Temporal 被限定为未来跨服务、长时间业务流程的外层编排。
- 修改原因：两套框架都提供恢复与重试，如果不先划定状态和失败所有权，会出现重复执行、双重 Checkpoint 与不可预测的重试叠加。
- 影响范围：LangGraph 状态必须在适配器内转换；Temporal Activity 未来只调用 Nexus Task 边界；同一业务状态只能有一个主数据源。
- 实施顺序：当前启用 Local 与 LangGraph Runtime，Temporal 在远程 Worker 契约和幂等语义完成后接入。

### 2026-07-18 · v0.15 · 从全 Python 规划演进为按负载拆分的多语言架构

- 修改内容：Python 保留智能平面；Go 承担 Task Runtime、Scheduler 与 MCP Gateway；Rust 承担低延迟 Skill 排序；TypeScript 构建 Studio；C++/CUDA 仅通过外部模型推理服务使用。
- 修改原因：Agent 逻辑需要 Python 生态，但高并发 IO、调度和批量排序具有不同运行特征；同时不能为了技术展示过早全面微服务化。
- 影响范围：Monorepo 增加跨语言服务；JSON v1 契约成为首个互操作边界；本地 Python 实现继续作为规范与降级路径。
- 拆分准入：只有基准证明远程实现收益覆盖网络、部署和运维成本时，生产配置才启用对应服务。

### 2026-07-18 · v0.16 · 固定远程 Task Runtime 的执行语义

- 修改内容：Go Runtime 负责有界并发、单次尝试超时、有限重试和基于幂等键的重复请求合并，使用 HTTP/JSON v1 接收任务。
- 修改原因：从进程内执行迁移到远程 Worker 后，网络重试可能产生重复副作用；同时需要独立控制并发以保护模型和工具下游。
- 影响范围：Python Orchestrator 仍拥有业务 DAG；Go 只拥有执行尝试；所有远程任务必须提供 `run_id`、`task_id` 与 `idempotency_key`。
- 协议取舍：先用可读 JSON 验证边界，只有基准证明有必要时再引入 gRPC。

### 2026-07-19 · v0.17 · 抽取 Rust Skill 排序内核

- 修改内容：新增 Rust 实现的策略过滤、RRF、加权排序、稳定排序和 Token 预算选择，仅接收候选元数据与标准化信号。
- 修改原因：大量候选的融合与排序是 CPU 密集路径，适合用内存安全的原生实现；同时需要避免远程服务直接读取 Skill 正文破坏渐进加载。
- 影响范围：Python 仍负责意图与领域理解；Qdrant 等适配器负责召回；Rust 只负责确定性排序；输出必须保留评分解释。
- 实施策略：先以无网络依赖 crate 建立算法与基准，确认收益后再选择 HTTP 或 gRPC 传输。

### 2026-07-19 · v0.18 · 分离 MCP 工具调用与 A2A 智能体互操作

- 修改内容：Go MCP Gateway 增加工具发现过滤、服务端授权、风险校验、超时、幂等和审计；A2A 保留为外部 Agent 生命周期协议。
- 修改原因：短时、Schema 驱动的工具动作与长时间外部 Agent 任务具有不同状态和取消语义，混在同一网关会造成授权与重试职责不清。
- 影响范围：Agent Manifest 的 Tool 白名单在 Gateway 再次执行；模型参数不构成权限；MCP 与 A2A 独立版本化，通过关联 ID 统一追踪。
- 当前范围：MCP Gateway 完成核心策略算法，A2A 先冻结架构边界，待企业 Runtime 阶段实现。

### 2026-07-25 · v0.19 · 统一运行遥测并默认保护上下文内容

- 修改内容：运行与任务生命周期输出统一结构化事件，使用 `run_id` 和 `task_id` 关联；本地适配器提供事件与计数器，生产环境映射 OpenTelemetry。
- 修改原因：多 Agent 链路跨越 Router、Runtime、Model 与 Tool，没有统一关联与命名就无法解释失败、成本和延迟。
- 影响范围：任务启动、成功和失败必须发事件；属性使用 `nexus.*`；Token 聚合进入指标；敏感键自动脱敏，长值截断。
- 隐私取舍：默认不记录完整提示词、Skill 指令、工具参数和产物正文，内容采样需要显式受控配置。

### 2026-07-26 · v0.20 · 将路由效果从演示升级为可重复基准

- 修改内容：建立版本化路由样本、All Skills、Keyword 与 Nexus Hybrid 三组策略，并统一计算 Top-1、Recall@K、MRR、NDCG 和选中指令 Token。
- 修改原因：单个示例“看起来选对了”不能证明路由质量，Token 降低也可能以召回下降为代价。
- 影响范围：路由改动必须运行同一数据集；公开结果必须携带代码、数据集、Skill 数、环境与模型版本；禁止把假设数字写成 Benchmark 结果。
- 当前数据：首批十条中英文 PRD 子任务用于回归，不代表生产统计显著性，后续扩展到多领域大规模数据。

### 2026-07-26 · v0.21 · 用结构化质量门替换固定 PRD 分数

- 修改内容：PRD 评审统一输出清晰度、完整性、可行性、一致性和证据覆盖，低于 85 或存在核心章节阻断时返回具体修订任务。
- 修改原因：固定分数或自由文本“检查通过”不可验证，也不能为 Replanner 提供可执行输入。
- 影响范围：运行结束时从实际 `PRD.md` 计算评审；Rubric 独立版本化；确定性阻断规则优先于未来模型评审。
- 后续验证：增加模型评审与人工盲评相关性实验，但不允许模型覆盖结构性阻断项。

### 2026-08-01 · v0.22 · 建立单机 VM 的可选基础设施栈

- 修改内容：Compose 加入 API、PostgreSQL、Redis、Qdrant、MinIO 与 NATS；OTel Collector、Prometheus 和 Grafana 作为可选 profile；配置统一使用 `NEXUS_*` 环境变量。
- 修改原因：参考内核需要一套接近企业分层、又能在个人 VM 中启动的集成环境；一次启用全部生产组件会把精力转移到运维。
- 影响范围：默认启动核心数据与消息服务；观测栈按需启用；镜像使用非 root 与只读根文件系统；生产环境拒绝示例密钥。
- 延后组件：Temporal、Kafka、Keycloak 与 Kubernetes 等到相应边界和负载出现后再加入。

### 2026-08-01 · v0.23 · 将运行数据拆为可回放的租户级记录

- 修改内容：PostgreSQL Schema 覆盖租户、主体、项目、资源版本、Run、Task、Agent Run、Skill/Tool 调用、路由结果、产物、Memory、模型用量和审计。
- 修改原因：只有最终 PRD 无法回答“为何选择这个 Agent/Skill、消耗多少 Token、哪个工具失败”，也不能在版本变化后回放。
- 影响范围：所有操作表带 `tenant_id`；资源版本不可原地覆盖；Tool 幂等键在租户内唯一；大对象与向量继续分别放 MinIO 和 Qdrant。
- 安全准备：Schema 为后续 Row Level Security 预留租户键，但本次单机阶段尚未启用数据库级策略。

### 2026-08-02 · v0.24 · 完成开发与生产容器边界设计

- 修改内容：补全逻辑容器、单机与生产拓扑、组件职责、依赖方向、四条核心数据流、扩缩容和生产强制能力。
- 修改原因：多语言代码出现后，需要明确每个进程拥有的数据与失败责任，避免把目标架构误认为当前已完成能力。
- 影响范围：文档逐项标明当前实现；开发模式默认进程内组合；生产远程服务必须先通过契约、负载和故障准入。
- 风险控制：Kubernetes、Temporal、OIDC 与策略系统仍是目标状态，不在文档中宣称已经生产就绪。

### 2026-08-02 · v0.25 · 增加 10,000 Skill 可重复规模基准

- 修改内容：增加确定性合成目录生成器，可测 100 到 10,000+ Skill 下的质量、p50/p95 延迟、峰值内存与上下文 Token 差异。
- 修改原因：七个真实 Skill 只能验证功能，不能暴露线性扫描和上下文规模问题；多语言拆分也需要同一负载作为准入证据。
- 影响范围：报告必须记录环境、目录大小、查询数、重复次数和生成器版本；Python 与 Rust Router 后续使用相同候选夹具比较。
- 解释限制：合成无关候选主要隔离规模开销，不代表真实困难负样本，不能据此宣称生产准确率。

### 2026-08-15 · v0.26 · 将 Reflection 落为有界结构化重规划

- 修改内容：评审失败维度映射为 Research、Requirements 或 Technical 修订任务，再重新 Write 与 Review；默认最多两轮。
- 修改原因：自由文本“再优化一下”既不可审计也可能无限消耗 Token，固定重跑全部任务又浪费已完成结果。
- 影响范围：每轮任务使用独立 ID 并进入同一 Run；API 暴露修订任务；最新 PRD 使用同一 Rubric 评审；达到上限后 Run 标记 `blocked`。
- 后续增强：生产 Replanner 将基于证据依赖复用未受影响结果，并允许人工批准额外迭代预算。

### 2026-08-15 · v0.27 · 把授权从提示词移到服务端策略边界

- 修改内容：统一 Principal、资源动作、风险和数据分类判断，返回 Allow、Deny 或 Require Approval 以及审计、审批和幂等等执行义务。
- 修改原因：Prompt Injection 或伪造客户端字段可能绕过“请勿调用危险工具”等软约束，执行权限必须由模型上下文之外的可信服务判断。
- 影响范围：Router 过滤只用于提前缩小候选；Tool、Memory、模型与外部 Agent Gateway 执行前必须再次授权；跨租户默认拒绝。
- 生产演进：本地规则引擎作为规范实现，后续 OPA 适配器复用同一决策契约。

### 2026-08-16 · v0.28 · 在数据库层强制租户隔离

- 修改内容：所有租户表启用并强制 PostgreSQL Row Level Security，读写均绑定事务级 `nexus.tenant_id`；Python 适配器只接受 UUID 并返回参数化设置语句。
- 修改原因：只依赖应用查询中的 `WHERE tenant_id` 容易因遗漏过滤造成跨租户泄露，连接池中的会话级设置还可能污染后续请求。
- 影响范围：每个业务事务必须先设置本地租户上下文；连接归还前必须结束事务；迁移和平台管理使用独立数据库角色。
- 验证方式：架构测试枚举所有带租户键的表，检查 Enable、Policy 和 Force 三项配置。

### 2026-08-16 · v0.29 · 将工程问题沉淀为独立演进记录

- 修改内容：新增持续维护的《问题与解决记录》，按开发测试、场景验证、基准测试和部署运行区分证据来源；每项保留现象、定位、根因、修复、验证、预防和遗留风险。
- 修改原因：架构演进文档适合说明系统为什么改变，却不能替代故障与使用问题的排查账本；把两者混在一起会丢失复现证据，也容易把预判风险写成真实事故。
- 影响范围：架构边界变化继续记入本文；实际确认的工程问题记入问题记录；同一修复同时改变架构时，两处相互对应但不重复大段内容。
- 执行约束：不得覆盖旧结论，根因变化时追加更正；不得把未运行的测试标为通过；问题只有具备可重复验证证据后才能标记“已解决”。

### 2026-08-22 · v0.30 · 先固定 Studio 可观测信息架构

- 修改内容：新增 TypeScript/Next.js Studio，首版只读展示运行列表、任务执行链、质量维度、Token 成本、产物与 Skill 路由解释。
- 修改原因：普通聊天界面无法回答“系统做了什么、为什么选这个 Skill、哪里失败、花费多少”；同时后端读模型尚未稳定，不应让前端直接依赖内部 `NexusState`。
- 影响范围：Studio 使用独立 TypeScript View Model 和明确标注的演示快照；后续 API 按页面查询需要提供摘要与详情投影，而不是暴露内部可变状态。
- 设计取舍：首版不提供暂停、重试、审批等写操作，避免在授权、幂等和审计边界完成前制造不可控副作用。

### 2026-08-22 · v0.31 · 为 Studio 增加稳定运行读模型

- 修改内容：运行记录补充起止时间、任务定义、路由解释和上下文预算；API 增加运行摘要列表与详情查询，开发模式使用有界内存投影。
- 修改原因：Studio 不能直接读取 `NexusState` 或数据库表，也不能从日志反推任务关系；查询契约需要与执行模型独立演进。
- 影响范围：每次运行结束后生成不可变 JSON 投影；列表不返回产物正文；内存存储限制 100 条并返回防御性副本；Orchestrator 仍不承担查询职责。
- 演进路径：当前读模型在进程重启后丢失，只用于本地联调；生产适配器将写入 PostgreSQL，并以事件序列重建同一 v1 投影。

### 2026-08-23 · v0.32 · 落地 A2A 外部任务生命周期内核

- 修改内容：增加 Agent Card、跨 Agent 消息、产物和外部任务模型，以显式状态机控制提交、工作、等待输入、等待授权、完成、失败、取消和拒绝。
- 修改原因：外部 Agent 任务可能跨越长时间和多次消息，不能映射为一次 MCP 调用；网络重复与乱序事件还可能让终态回退或重复消费结果。
- 影响范围：A2A Gateway 未来只接受规范状态迁移；终态不可回退；重复状态事件幂等；租户、上下文与关联 ID 成为必填审计边界。
- 当前边界：本次只实现协议无关模型、JSON Schema 与本地状态机，远程 Client/Server 和流式传输仍需真实互操作环境验证。

### 2026-08-23 · v0.33 · 用跨语言 CI 统一可验证证据

- 修改内容：增加 Python、两个 Go 服务、Rust Router、TypeScript Studio、文档和 Compose 六类 CI Job，并提供不依赖外部服务的仓库级契约验证器。
- 修改原因：开发机缺少部分工具链时只能证明源码结构正确，无法证明编译、类型、Race Detector 和生产构建通过；多语言边界需要同一合并门。
- 影响范围：各语言固定主版本并分别缓存；Python 承担跨仓库 Schema、Manifest、导航和演进账本一致性检查；任何 Job 失败都会阻止主分支验证通过。
- 证据约束：工作流配置合入不代表远程运行成功；只有可追踪的首次 CI 结果通过后，才能关闭对应工具链问题。

### 2026-08-29 · v0.34 · Studio 采用 API 优先且可见降级的数据源

- 修改内容：Studio 服务端页面读取稳定运行查询 API，设置 2.5 秒超时；未配置或请求失败时回退到本地快照，并在总览和详情持续显示数据来源及告警。
- 修改原因：开发环境中 API 与 Studio 经常独立启动，完全失败会阻塞界面开发，静默回退又会让用户把演示指标误认成真实运行。
- 影响范围：Python 投影通过 TypeScript 边界适配后进入 UI，未知状态采取失败优先映射；API 地址只在服务端读取，不暴露内部容器地址。
- 取舍：降级保证只读可用性，不允许基于快照执行重试、审批或取消；生产环境后续可配置为 API 不可用时直接失败。

### 2026-08-29 · v0.35 · 上下文分区从硬配额演进为可审计软上限

- 修改内容：System 与 Task 等必要片段可以借用其他未使用的输入分区，普通片段仍按分类上限裁剪；报告新增借用 Token 数。
- 修改原因：硬分区会在全局窗口尚有空间时拒绝较长但不可丢失的用户任务，导致合法复杂需求无法启动；完全取消分区又会让召回内容挤占输出预算。
- 影响范围：全局输入预算和输出预留仍是硬上限；只有显式 `required` 片段可以借用；API 暴露借用量用于 Studio 和容量告警。
- 观察指标：如果某类上下文长期借用，应该调整分类比例或拆分任务，而不是无限提高总窗口。

### 2026-08-30 · v0.36 · 模型降级受错误类型和数据边界约束

- 修改内容：模型目标声明物理模型与允许的数据分类；Gateway 只对连接失败、超时、429 和 5xx 执行顺序降级，永久拒绝与协议错误立即失败。
- 修改原因：把所有异常都重试会放大无效请求、成本和延迟，还可能为了可用性把受限数据发送给不合规供应商。
- 影响范围：Agent 继续使用逻辑模型名；Gateway 为每个目标替换物理模型；每个目标至多尝试一次；所有瞬时目标失败后返回聚合诊断。
- 遗留取舍：供应商接收请求后发生超时仍可能造成双份计费，生产适配器需要请求 ID、尝试级遥测和费用对账。

### 2026-08-30 · v0.37 · Memory 检索前置租户、分类与有效期过滤

- 修改内容：Memory 条目补充租户、来源 Run、来源类型、数据分类和过期时间；端口要求每次读写显式传递租户，检索先过滤再排序。
- 修改原因：只保存纯文本会失去来源和保留策略，全局召回后再过滤还可能把其他租户或受限内容暴露给排序与日志链路。
- 影响范围：Orchestrator 从运行入口向 Memory 端口传播可信租户；默认检索只允许 Public/Internal 且排除过期数据；本地 CLI 使用 `local` 兼容租户。
- 生产约束：API 不能接受请求体自报租户；PostgreSQL/Qdrant 适配器必须在服务端或索引分区执行同样过滤，并通过 RLS 与隔离测试。

### 2026-09-05 · v0.38 · 分离进程存活与接流准备状态

- 修改内容：API 增加独立 `/livez` 和 `/readyz`；Readiness 并发执行有界依赖检查，关键失败返回 503，可选失败只标记降级；旧 `/healthz` 保留为弃用别名。
- 修改原因：单一永远返回 200 的健康端点会让编排平台把“进程活着”和“可以处理请求”混为一谈，依赖故障时继续送流量或反复重启健康进程。
- 影响范围：Compose 使用 Readiness 作为容器健康检查；检查异常只公开类型，不泄露连接字符串；运行手册分别定义摘流与重启动作。
- 后续演进：生产适配器还需注册 PostgreSQL、Redis、模型策略与消息系统检查，并为检查结果增加缓存，避免探针本身放大下游负载。

### 2026-09-05 · v0.39 · 将环境前置条件变成可执行诊断

- 修改内容：CLI 新增 `doctor`，统一检查 Python、配置、Agent/Skill 清单、可选 Python 模块和 Go/Rust/Node/Docker 命令；PRD 命令支持显式仓库根目录。
- 修改原因：缺少工具链、从错误目录运行和生产示例凭据过去只在构建或首次请求时暴露，错误表现分散且容易被误判为业务失败。
- 影响范围：关键条件缺失返回非零；可选能力缺失默认 `degraded` 但核心 CLI 可用；严格模式要求全部通过；JSON 报告不包含密钥和异常正文。
- 打包取舍：当前 Agent/Skill 仍是仓库级可版本化资产，不复制进 Python Wheel；未来远程 Registry 上线后再取消 `--root` 要求。

### 2026-09-06 · v0.40 · 将能力声明绑定到可重复证据

- 修改内容：项目状态统一区分设计完成、核心实现、本地验证、CI 验证、集成验证和生产可用，并逐项声明当前限制与下一准入条件。
- 修改原因：多语言源码出现后，“仓库里有实现”不等于“当前机器已编译”，更不等于“生产可用”；含糊表述会让部署和技术决策建立在错误证据上。
- 影响范围：README、状态页和路线图使用同一证据口径；合成基准、实际 HTTP 测试、外部服务联调和生产准入不得互相替代。
- 复杂度控制：Kubernetes、Temporal 与 OIDC 继续作为目标架构，只有远程服务、持久恢复和身份边界达到准入条件后才增加部署实现。

### 2026-09-09 · v0.41 · 冻结 v0.1 外部契约并发布参考基线

- 修改内容：将现有 Runtime、Router、MCP 与 A2A v1 JSON Schema，以及 Python 公共入口和 Studio 读模型，确定为 v0.1 系列的兼容基线；统一 Python 与 Studio 版本为 0.1.0。
- 修改原因：Go Runtime、Rust Router、Studio 和未来外部 Agent 都依赖跨进程边界；如果首个版本发布后仍可无版本地改变字段，任何互操作验证都无法沉淀为可重复证据。
- 影响范围：v0.1 后续补丁和次要版本只允许向 v1 契约增加可选字段或修复实现；删除、改名、收紧必填项等破坏性变化必须发布 v2，并在迁移窗口内保留双读或转换器。
- 验证方式：发布审计解析全部 Schema 与 Manifest、执行 Python 测试和静态检查、严格构建文档站，并从源码生成 0.1.0 Wheel；多语言编译与远程互操作仍明确列为下一阶段准入条件。
- 设计取舍：本次冻结的是参考实现边界，不是生产就绪承诺；未通过真实环境验证的 Kubernetes、Temporal、OIDC、远程 MCP/A2A 和持久恢复不会为追求版本完整度而提前加入。

## 2026-09-10：以持久化 PRD 写作为首个实际产品闭环

修改：新增 `nexusos.prd` 应用服务和独立的文档/版本/任务 API；Studio PRD 页面切换到真实接口。复用 AgentResolver、Skill Router、渐进 Skill 加载、Model Gateway 和 LangGraphRuntime。原离线编排 API 保留作参考基线。

原因：原页面是定时器和固定记账内容，后端参考 Runtime 也只能生成固定模板；不足以编写真实 PRD。原参考上下文采用片段预算，不能让正式写作静默遗漏原始约束和材料。

边界：正式写作把用户简报和原始材料作为所有阶段的必需上下文；超预算显式失败。完整草稿在模型评审前持久化；评审失败不删除正文。修订基于最新正文，历史版本只追加。任务和内容状态分离，模型评审不等同于业务批准。

存储取舍：首版个人工作区使用 SQLite，文档/版本/任务变更在事务中执行，修订号防止多页面覆盖。单 API 实例管理内存任务队列，重启后中断任务不自动重放。企业 PostgreSQL、多实例调度和租户隔离仍是后续范围；不能把现有基础设施的存在当成此工作区已接入它们。

模型配置：通过服务端环境变量指定兼容服务及模型；无配置时保持编辑能力但禁用生成，不降级到假内容。输入和输出预算可按模型配置。

## 2026-09-10：PRD 输入、执行与产物建立持久追溯关系

修改：新增 `prd_trace_events` 只追加事件表、SHA-256 内容对象表；业务保存与事件同事务。任务固定输入快照，版本关联 origin_job_id 与父版本；`TracedModelGateway` 在真实调用前后记录输入和结果，LangGraphRuntime 传递 Span 标识，兼容模型 HTTP 请求发送 W3C traceparent。Studio 增加历史任务、文档时间线与 JSON 导出。

原因：已有任务 JSON 和最后任务指针不足以回答“当时用了哪些材料、请求过哪个模型、失败在哪里、这个版本怎么来的”。模型截断虽然正确判失败，但原响应也需要留作排查依据。

取舍：完整材料进入本机受控追溯存储，不进入一般遥测摘要。只追加 trigger 防止应用误改，不宣称防管理员篡改；本期使用关联标识，不宣称已接 OTel Collector 或实现全平台分布式追踪。自动重试、断点恢复和调度仍按独立台账推进。

详细记录：[RT-001](../development/runtime-issues/001-end-to-end-tracing.md)。

## 2026-09-10：确认 OpenTelemetry 与 Langfuse 的后续接入方向

决策：根据用户确认，采用 OpenTelemetry 统一采集，Langfuse 承接模型追踪与评测，同时保留现有 SQLite 业务追溯和 Studio 查询入口。当前仅记录选型与待办，尚未安装或启用相关服务。

原因：现有业务事件承担文档与执行证据的事务一致性；跨服务调用分析和评测实验管理使用成熟平台，并通过统一标识关联。

影响与取舍：后续在 observability、Workflow、Runtime 与 Gateway 边界接入，明确异步任务上下文及模型调用去重。外部遥测导出应有界且不阻塞业务保存；模型正文上报需配置目的地与访问控制。保留本地权威业务记录，增加专业追踪平台的部署、数据映射和运维成本。

接入清单与完成标准见[关键运行问题台账 RT-001](../development/critical-runtime-backlog.md)。


## 2026-09-10：加入单实例 PRD 检查点恢复

修改：LangGraphRuntime 接入 AsyncSqliteSaver，按任务和阶段隔离节点状态；业务阶段成功、证据与用量同事务保存，正文发布与去重标记同事务。API 与 Studio 区分重新执行和继续执行，继续固定原始输入，继承阶段来源并拒绝过期现场。

原因：真实任务在已有成功分析后截断，原有追溯虽能解释失败，却不能利用中间结果继续运行。

取舍：保留当前分段写作流程，按阶段复用，不恢复 Python 栈或半条模型响应。原生检查点和业务存储是两个提交边界；已持久节点可以弥补后续业务提交失败，未知远端结果仍可能重调。部署仍为单实例，依赖新增 langgraph-checkpoint-sqlite，备份新增检查点数据库。

验证与详细故障语义：[RT-003 检查点恢复](../development/runtime-issues/003-checkpoint-recovery.md)。
